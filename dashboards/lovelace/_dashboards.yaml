# GENERATED by scripts/export_dashboards.py - DO NOT HAND-EDIT.
# Source: .storage/lovelace_dashboards (UI-managed; HA loads that, never this file).
# Restore: dashboard > three-dot menu > Raw configuration editor,
#          select all, replace with everything below this header.
# This is a REGISTRY, not a dashboard: it records which dashboards
# exist / which custom card resources load. Not pasteable.
#
items:
  - id: map
    icon: mdi:map
    title: Map
    url_path: map
    show_in_sidebar: true
    require_admin: false
    mode: storage
  - id: energy_performance
    show_in_sidebar: true
    title: Energy Performance
    require_admin: false
    mode: storage
    url_path: energy-performance
  - id: lovelace
    icon: mdi:view-dashboard
    title: Overview
    url_path: lovelace
    show_in_sidebar: true
    mode: storage
    require_admin: false
  - id: sdr_utility_meters
    show_in_sidebar: true
    icon: mdi:radio-tower
    title: SDR Utility Meters
    require_admin: false
    mode: storage
    url_path: sdr-utility-meters

# Runtime Total Today - Mushroom Template Card
# Shows total zone demand runtime in hours
# Dashboard: Energy Performance - Operations
type: custom:mushroom-template-card
primary: "{{ states('sensor.hvac_total_heat_runtime_today') | round(2) }}"
secondary: Total Zone Demand (sum)
icon: mdi:clock-outline
color: green
features_position: bottom
vertical: true
grid_options:
  columns: 3
  rows: 3

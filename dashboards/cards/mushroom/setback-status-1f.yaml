# 1F Setback Status
# Shows if setback is active and the current depth
# Dashboard: Energy Performance - Operations
type: custom:mushroom-template-card
primary: >-
  {% if is_state('input_boolean.hvac_1f_setback_active', 'on') %}
    {% set depth = (states('input_number.hvac_1f_hold_setpoint') | float(67) -
                    states('input_number.hvac_1f_setback_setpoint') | float(62)) | round(0) %}
    Setback {{ depth }}°F
  {% else %}
    No Setback
  {% endif %}
secondary: 1F Tonight
icon: mdi:home-floor-1
color: >-
  {% if is_state('input_boolean.hvac_1f_setback_active', 'on') %}blue
  {% else %}grey{% endif %}
features_position: bottom
vertical: true
tap_action:
  action: more-info
  entity: input_boolean.hvac_1f_setback_active
grid_options:
  columns: 6
  rows: 2
